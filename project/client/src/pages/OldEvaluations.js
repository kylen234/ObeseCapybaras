import React, { Component } from 'react';
import {SearchBar} from '../components';

class OldEvaluations extends Component{
    render(){
        return(
            <SearchBar />
        )
    }
}

export default OldEvaluations;